---
const toolNames = [
  { name: 'Figma', accent: '#2563EB' },
  { name: 'Sketch', accent: '#7BAAF7' },
  { name: 'Webflow', accent: '#E5A93C' },
  { name: 'Photoshop', accent: '#2563EB' },
  { name: 'Illustrator', accent: '#E5A93C' },
  { name: 'Hotjar', accent: '#7BAAF7' },
  { name: 'Maze', accent: '#2563EB' },
  { name: 'Clarity', accent: '#E5A93C' },
  { name: 'Miro', accent: '#7BAAF7' },
  { name: 'Google Analytics', accent: '#2563EB' },
  { name: 'Data Analytics', accent: '#E5A93C' },
  { name: 'Power BI', accent: '#7BAAF7' },
  { name: 'HTML & CSS', accent: '#2563EB' },
  { name: 'VS Code', accent: '#E5A93C' },
  { name: 'GitHub', accent: '#7BAAF7' },
  { name: 'Jira & Asana', accent: '#2563EB' },
]

// Evenly distributed around the photo on an ellipse, alternating radius so
// adjacent labels (which vary a lot in length) don't collide.
const tools = toolNames.map((t, i) => {
  const angle = (i / toolNames.length) * Math.PI * 2 - Math.PI / 2
  const radius = i % 2 === 0 ? 44 : 50
  const left = 50 + radius * Math.cos(angle)
  const top = 50 + radius * Math.sin(angle)
  return { ...t, left: left.toFixed(2), top: top.toFixed(2), delay: (i % 6) * 0.35 }
})

const pillars = [
  {
    icon: '◇',
    label: 'Misión',
    text: 'Ayudar a negocios del norte del Perú a crecer con presencia digital que realmente funciona: sitios rápidos, claros y pensados para convertir, sin depender de un proyecto nuevo cada vez que necesitan un cambio.',
    accent: '#2563EB',
  },
  {
    icon: '✧',
    label: 'Visión',
    text: 'Ser el estudio de referencia en el norte del Perú para negocios que quieren una web profesional, sin las complicaciones ni los costos de una agencia tradicional: cercano, ágil y con resultados medibles.',
    accent: '#E5A93C',
  },
]
---

<section id="sobre-alo" class="py-28 px-6 relative overflow-hidden">
  <div
    class="pointer-events-none absolute inset-0"
    aria-hidden="true"
    style={{
      background:
        'radial-gradient(600px circle at 12% 15%, rgba(37,99,235,0.14), transparent 60%), radial-gradient(520px circle at 88% 85%, rgba(229,169,60,0.12), transparent 60%)',
    }}
  />

  <div class="max-w-6xl mx-auto relative">
    <div data-reveal class="opacity-0 translate-y-6 mb-16 max-w-2xl">
      <span class="text-xs font-semibold tracking-widest uppercase mb-4 block" style={{ color: '#7BAAF7', fontFamily: 'Inter, sans-serif' }}>
        — Sobre Alo
      </span>
      <h2 class="heading font-extrabold leading-tight" style={{ fontSize: 'clamp(2rem, 4vw, 3.2rem)', letterSpacing: '-0.02em' }}>
        La persona detrás de<br />
        <span style={{ color: '#2563EB' }}>Alo Studio</span>
      </h2>
    </div>

    <div data-reveal class="opacity-0 translate-y-6 flex flex-col items-center mb-16">
      {/* Desktop: photo with tags orbiting around it */}
      <div class="relative hidden lg:block w-[38rem] h-[38rem] max-w-full">
        <div
          class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[15rem] h-[15rem] rounded-full overflow-hidden flex items-center justify-center"
          style={{ background: 'linear-gradient(135deg, #2563EB 0%, #7BAAF7 100%)', border: '1px solid rgba(255,255,255,0.12)' }}
        >
          <img
            src="/images/cynthia-alondra.jpg"
            alt="Cynthia Alondra León Abal, fundadora de Alo Studio"
            class="w-full h-full object-cover"
            loading="lazy"
            onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'"
          />
          <span
            class="hidden w-full h-full items-center justify-center heading font-extrabold"
            style={{ fontSize: '3.5rem', color: 'rgba(255,255,255,0.85)' }}
          >
            CA
          </span>
        </div>

        {tools.map((t) => (
          <div
            class="absolute -translate-x-1/2 -translate-y-1/2 animate-float"
            style={{ top: `${t.top}%`, left: `${t.left}%`, animationDelay: `${t.delay}s` }}
          >
            <span
              class="whitespace-nowrap px-4 py-2 rounded-full text-sm font-bold"
              style={{
                background: 'rgba(15,19,28,0.9)',
                border: `1px solid ${t.accent}55`,
                boxShadow: `0 0 20px ${t.accent}33`,
                color: t.accent,
                fontFamily: 'Plus Jakarta Sans, sans-serif',
                backdropFilter: 'blur(6px)',
              }}
            >
              {t.name}
            </span>
          </div>
        ))}
      </div>

      {/* Mobile / tablet: photo, then tags wrap below it */}
      <div class="lg:hidden flex flex-col items-center gap-8">
        <div
          class="w-[13rem] h-[13rem] sm:w-[15rem] sm:h-[15rem] rounded-full overflow-hidden flex items-center justify-center"
          style={{ background: 'linear-gradient(135deg, #2563EB 0%, #7BAAF7 100%)', border: '1px solid rgba(255,255,255,0.12)' }}
        >
          <img
            src="/images/cynthia-alondra.jpg"
            alt="Cynthia Alondra León Abal, fundadora de Alo Studio"
            class="w-full h-full object-cover"
            loading="lazy"
            onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'"
          />
          <span
            class="hidden w-full h-full items-center justify-center heading font-extrabold"
            style={{ fontSize: '3rem', color: 'rgba(255,255,255,0.85)' }}
          >
            CA
          </span>
        </div>

        <div class="flex flex-wrap justify-center gap-3 max-w-xl">
          {tools.map((t, i) => (
            <span
              class="animate-float whitespace-nowrap px-4 py-2 rounded-full text-sm font-bold"
              style={{
                animationDelay: `${(i % 6) * 0.35}s`,
                background: 'rgba(15,19,28,0.9)',
                border: `1px solid ${t.accent}55`,
                boxShadow: `0 0 20px ${t.accent}33`,
                color: t.accent,
                fontFamily: 'Plus Jakarta Sans, sans-serif',
                backdropFilter: 'blur(6px)',
              }}
            >
              {t.name}
            </span>
          ))}
        </div>
      </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-[1.1fr_0.9fr] gap-8 items-start">
      <div
        data-reveal
        class="opacity-0 translate-y-6 rounded-3xl p-8 md:p-10"
        style={{ background: 'rgba(255,255,255,0.04)', border: '1px solid rgba(255,255,255,0.07)' }}
      >
        <p class="text-white/70 leading-relaxed" style={{ fontFamily: 'Inter, sans-serif', fontSize: '1.05rem' }}>
          Soy Cynthia Alondra León Abal, diseñadora UX/UI y fundadora de Alo Studio. Empecé en diseño gráfico en 2016 y
          desde entonces trabajé en proyectos para bancos, retailers, inmobiliarias y startups, desde el rediseño de
          la banca por internet de un banco internacional hasta e-commerce para marcas en Latinoamérica y
          Norteamérica. Esa experiencia levantando requerimientos, validando flujos de usuario y coordinando equipos
          multidisciplinarios es lo que traje a Alo Studio: no diseño por diseñar, sino soluciones pensadas para que
          el negocio de cada cliente funcione mejor.
        </p>
      </div>

      <div class="flex flex-col gap-6">
        {pillars.map((p, i) => (
          <div data-reveal class="opacity-0 translate-y-6">
            <div
              class="animate-float rounded-2xl p-6"
              style={{
                background: 'rgba(255,255,255,0.04)',
                border: `1px solid ${p.accent}33`,
                boxShadow: `0 0 40px ${p.accent}22`,
                animationDelay: `${i * 0.8}s`,
              }}
            >
              <div class="flex items-center gap-3 mb-3">
                <span
                  class="w-9 h-9 rounded-full flex items-center justify-center text-base"
                  style={{ background: `${p.accent}1A`, color: p.accent }}
                >
                  {p.icon}
                </span>
                <span class="heading font-bold text-sm uppercase tracking-wide" style={{ color: p.accent }}>{p.label}</span>
              </div>
              <p class="text-white/60 text-sm leading-relaxed" style={{ fontFamily: 'Inter, sans-serif' }}>{p.text}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<script>
  import { gsap, ScrollTrigger, prefersReducedMotion } from '@/lib/gsap'

  const section = document.querySelector('#sobre-alo')
  if (section) {
    const revealEls = section.querySelectorAll<HTMLElement>('[data-reveal]')

    if (prefersReducedMotion()) {
      gsap.set(revealEls, { opacity: 1, y: 0 })
    } else {
      gsap.to(revealEls, {
        opacity: 1,
        y: 0,
        duration: 0.6,
        ease: 'power3.out',
        stagger: 0.1,
        clearProps: 'transform',
        scrollTrigger: { trigger: section, start: 'top 80%', once: true },
      })

      window.addEventListener('load', () => ScrollTrigger.refresh())
    }
  }
</script>
